import numpy as np
import matplotlib.pyplot as plt

def spectrogram(y, w=np.array([]), nstep=0):  # y is a (100000, 1) matrix
   
     # area and height normalizations are the same for a rectangular window
     # half max points
    # window center

      # y[i] change the dimension of y to 2
   # "forward" transform in the lecture note
     # only half the data needed for a real signal
    
    # normalized frequency
    # time at window center
    return (yt, f, t)

def normalize_area(w):
    return 

def normalize_height(w):
    return 

def flattop(n):
    
    return w

def find_index(x, xcut):
  
    return np.searchsorted(xm, xcut)

def plot2(x, y, z, cmap_name='jet', interpolation='nearest', clabel=''):
   
